def soma_simetricos(valores):
    meio = 0
    if len(valores) % 2 == 0:
        indice = int(len(valores / 2))
        meio = valores[indice]
        valores.pop(indice)
    
    lista
    soma = 0
    for i in range(len(valores)):
        e1 = valores[i]
        e2 = valores[(i + 1)* (-1)]


